/**
 * CommissionsPlaceholder.tsx
 *
 * Placeholder for the Commissions tab (F21) — used in MF mode (RevenuePage).
 */

import { PlaceholderPage } from '../PlaceholderPage';

export function CommissionsPlaceholder() {
  return <PlaceholderPage title="Commissions" description="Commission calculation and validation — Coming soon" />;
}
