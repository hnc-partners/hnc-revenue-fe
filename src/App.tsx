/**
 * App.tsx
 *
 * Root application component for Revenue Frontend.
 * This file exists for compatibility but the main app setup is in main.tsx.
 *
 * For Module Federation:
 * - The shell provides the router context via RouterProvider
 * - Individual components (RevenuePage, etc.) are exposed separately
 */

export default function App() {
  return null;
}
